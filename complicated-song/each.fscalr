func iterate(sequence)
  foreach(n) in sequence
    n.eighth
  end
end

func iterate2(sequence)
  foreach(n) in sequence
    n.half
  end
end

func iterate3(sequence)
  foreach(n) in sequence
    n.quarterT
  end
end
