g = 1 * 2 + 3 * (5 + 5) / 5
seq = [note.pitch(A1), note.pitch(A2), note.pitch(A3)]
while(g++ < 10)
	seq += note.pitch(+g)
end
foreach(n) in seq
	n.volume(+5)
end
while(i < 4)
	x = i * 2 - 1 /* testing with a comment in the mix */
	i++
end
seq = [ note, note.pitch(+5) ]
