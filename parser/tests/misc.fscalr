func nth_different( nth)
/* comment, son! */
     nth= []
     irk+= 1
     while(irk<nth*2)
	if(i%nth==0)
		myseq += note.pitch(A3).quarter
	else
		myseq += note.pitch(C3).quarter
	end
     	i+=1
     end
     nth
end

